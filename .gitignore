# Build trees (build-local = laptop, build = Orange Pi convention)
build*/

# Editor / OS noise
*.swp
*~
.DS_Store

# Packaged port zip (regenerated from portmaster/port/ + YTC.sh; duplicates tracked files)
portmaster/*.zip

# Runtime / user state (not source)
config/cast.json
downloads/
