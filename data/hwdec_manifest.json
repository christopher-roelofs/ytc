{
  "version": 1,
  "_contract": {
    "detect": "how the device qualifies: v4l2-stateful | v4l2-stateless | devnode:<path>",
    "check": "libavcodec decoder name proving the bundle libs are loaded",
    "hwdec": "mpv hwdec value the app uses for Hardware mode",
    "order": "first matching bundle wins",
    "files": "download list; EMPTY = the backend ships in the base bundle (setup records builtin, nothing to fetch)"
  },
  "bundles": {
    "v4l2": {
      "desc": "ffmpeg 6.1 with v4l2m2m — BUILT INTO the base libs.aarch64 (no download); any STATEFUL v4l2 decoder (Qualcomm Venus/Iris, Amlogic meson vdec)",
      "detect": "v4l2-stateful",
      "check": "h264_v4l2m2m",
      "hwdec": "v4l2m2m-copy",
      "files": []
    }
  }
}
